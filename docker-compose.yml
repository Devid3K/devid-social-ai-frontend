version: "3.9"

services:
  frontend:
    build:
      context: .
      dockerfile: Dockerfile
      target: production
    container_name: social-ai-frontend
    ports:
      - "80:80"
    restart: always
